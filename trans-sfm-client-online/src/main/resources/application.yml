server:
  port: 8080

spring:
  application:
    name: trans-sfm-client-online
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 # Nacos服务器地址

# 日志配置
logging:
  level:
    com.example.transsfmclient: debug
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n"