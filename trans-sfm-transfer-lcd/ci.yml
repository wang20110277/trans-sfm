Image:
  type: default
BeforeBuild:
  script:  #构建前脚本
Build:
  script: ./build.sh   #构建脚本，一般只写构建脚本即可
AfterBuild:
  script:  #构建后的脚本
Package:
  script:
  artifacts:
    name: lcd
    version: $COMMIT_ID-$BUILD_ID
    files: [./output]